server:
  port: 8084
spring:
  application:
    name: trending-movies-service  # ← FIXED (was ratings-service)
  # --- MYSQL (JPA) CONFIGURATION ---
  datasource:
    url: jdbc:mysql://localhost:3307/ratings_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Atoof000000
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  # --- MONGODB CONFIGURATION ---
  data:
    mongodb:
      uri: mongodb://localhost:27017/ratings_mongo_db
  # --- FLYWAY DISABLE ---
  flyway:
    enabled: false
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/